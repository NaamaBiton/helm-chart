---
extends: default

rules:
  line-length:
    max: 120

  braces:
    min-spaces-inside: 0
    max-spaces-inside: 1

  truthy:
    allowed-values: ['true', 'false', 'on', 'off', 'yes', 'no']

  comments:
    min-spaces-from-content: 1

ignore: |
  index.yaml
  charts/generic-app/templates/

---
apiVersion: v1
kind: Namespace
metadata:
  name: {{.Values.environment}}-{{.Values.projectName}}
  labels:
    {{- include "generic-app.labels" (dict "Chart" .Chart "Release" .Release "Values" .Values "component" "namespace") | nindent 4 }}
